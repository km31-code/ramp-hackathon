# Server-only. Never prefix this with NEXT_PUBLIC_.
OPENAI_API_KEY=

# Pick and pin a current text model before demo day.
OPENAI_MODEL=

# Keep the starter deterministic until the engine lane is ready.
HEIST_MODE=mock
